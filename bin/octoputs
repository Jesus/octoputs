#!/usr/bin/env ruby

require 'bundler/setup'
require 'octoputs'

app = lambda do |env|
  sleep 0.05
  [
    200,
    { 'Content-Type' => 'text/plain' },
    "This is a cool Rack-compatible application"
  ]
end

server = Octoputs.new app
server.listen
